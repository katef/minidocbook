
\startluacode

--function mdb_synopsis(t)
--	xml.flush(t)
--	context(string.len(xml.textonly(t)))
--	for s in string.gmatch(xml.textonly(t), ".*$") do
--		context(string.len(s or ""))
--		context.crlf()
--	end
--end

\stopluacode

% XXX: startlines does not work
\definestartstop[mdbsynopsis][
   style=mono,
   before={
      \startlines
      \noindent},
   after={\stoplines
      \indenting[next]}]
\startxmlsetups xml:synopsis
	\startmdbsynopsis
	\xmlflushspacewise{#1} % XXX: doesn't seem quite right
	\stopmdbsynopsis
\stopxmlsetups

