
\startluacode

--function mdb_synopsis(t)
--	xml.flush(t)
--	context(string.len(xml.textonly(t)))
--	for s in string.gmatch(xml.textonly(t), ".*$") do
--		context(string.len(s or ""))
--		context.crlf()
--	end
--end

\stopluacode

\startxmlsetups xml:synopsis
	% XXX: \starttyping does not work here
	{\tt \xmlflush{#1}}
\stopxmlsetups

\startxmlsetups xml:para
	\xmlflush{#1}
	\par
\stopxmlsetups

